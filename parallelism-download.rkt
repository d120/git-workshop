;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname parallelism) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; pi constant
(define PI 3)

;; area-circle: number -> number
;; Calculates the area of a circle.
(define (area-circle a)
;; Implement this
)

;; volume-sphere: number -> number
;; Calculates the volume of a sphere.
(define (volume-sphere a)
;; Implement this
)
